{
  "name": "qs-candidate-academy",
  "private": true,
  "version": "1.0.0",
  "description": "Landing page scaffold for QS Candidate Academy.",
  "type": "module",
  "scripts": {
    "format": "node scripts/verify.js format",
    "lint": "node scripts/verify.js lint",
    "build": "node scripts/verify.js build"
  },
  "engines": {
    "node": ">=18"
  }
}